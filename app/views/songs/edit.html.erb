<% unless @song.valid? %>
<ul>
    <% i = 0 %>
    <% while i < @song.errors.full_messages.length do %>
   <%=  the_mess = @song.errors.full_messages[i] %>
   <% i += 1%>
   <% end %>
   <%# <ol> %>
   <%# <%= the_mess %> 
   <%# </ol> %>
</ul>
<% end %>

<%= form_for(@song) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :released %>
  <%= f.check_box :released %>

  <%= f.label :released_year %>
  <%= f.text_area :released_year %>

  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>

  <%= f.label :genre %>
  <%= f.text_field :genre %>

  <%= f.submit %>

  <% end %>